server:
  port: 9091
spring:
  application:
    name: test
  cloud:
    consul:
      host: 192.168.10.180
      config:
        format: YAML
        enabled: true
        data-key: configuration
        prefix: config
        default-context: ${spring.application.name}
      port: 18500
      discovery:
        enabled: true
        service-name: test-datasource
        health-check-path: /actuator/health
        health-check-interval: 10s
        health-check-url: http://192.168.14.32:9091/actuator/health
        tags: dev